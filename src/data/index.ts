import {parts, findPartFromQuestion} from "./parts";
import {readingScore, listeningScore} from "./scores";

export default {parts, findPartFromQuestion, readingScore, listeningScore};
export {parts, findPartFromQuestion, readingScore, listeningScore};